<%= form_for @point do |f| %>
  <%= f.label :user_id %><br>
  <%= f.collection_select :user_id, User.all, :id, :username %>
  <%= f.label :number %>
  <%= f.number_field :number %>
  <%= f.submit 'Delete Points', method: delete %>
<% end %>
